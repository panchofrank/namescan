machine:
  database:
    override:
      - mongodb
test:
  post:
    - mkdir -p $CIRCLE_TEST_REPORTS/junit/
    - find . -type f -regex ".*/target/surefire-reports/.*xml" -exec cp {} $CIRCLE_TEST_REPORTS/junit/ \;
deployment:
  production:
    branch: master
    commands:
      - scp target/*.jar circleci@217.182.204.236:/.
  staging:
    branch: develop
    heroku:
      appname: boiling-garden-88107
